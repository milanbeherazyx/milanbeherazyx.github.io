# Skill: update-metrics

> STATUS: skeleton — written fully in Phase 5 (exact paths, filled frontmatter
> example, validation steps, common failure modes).

- Entry point: read `AGENTS.md` repo map first.
- Verify after any edit: `npm run build`
- Rollback: `git checkout -- <file>`
